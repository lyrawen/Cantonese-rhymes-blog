# 粤韵志 · 项目复盘

> Cantonese Rhymes Blog — 一个面向粤文化的博客系统，从 0 到 1 的完整过程复盘。
> 本文记录项目缘起、需求与方案、开发中的难点与解法、最终交付，以及一路走来的经验总结。

---

## 一、项目缘起

我出生在粤语（两广）地区。做「粤韵志」的本意，就是把家乡的文化介绍给更多人——它本质上是一个**文化博客**。

不论你是不是岭南人，只要愿意分享对岭南文化的看法、见解或介绍，都可以在这里发声。文化博客的本质就是文化交流。除了文章本身，我还为它加上了两条让「非母语者也能感受粤文化」的路径：

- **粤语朗读**：为文章提供粤语朗读，让想学粤语的人能「听」到粤文化；
- **粤文化 AI 助手**：外接一个 AI，回答读者关于岭南文化的疑惑、补充知识细节。

## 二、需求分析

一个基本的内容社区网站，应当满足用户「表达与交流」的需求：

- 发布内容：撰写文章，发表对岭南文化的看法与介绍；
- 互动阅读：阅读、点赞、评论、收藏、关注作者；
- 用户体系：注册与登录、个人主页、私信沟通；
- 扩展体验：粤语朗读、粤文化 AI 问答；
- 运营管理：管理员后台，对内容与用户进行统一管理。

## 三、方案设计

方案层面主要回答了三个问题：**用什么技术栈、长什么样、怎么注册登录**。

### 技术栈

- **后端**：Spring Boot —— 生态成熟、配置简便，对个人开发者友好，上手成本可控；
- **前端**：Vue —— 学习曲线相对平缓，便于快速搭建界面；
- **基础设施**：MySQL（数据）、Redis（缓存）；
- **第三方能力**：DeepSeek（AI 对话）、百度翻译（粤普互译）、百度语音合成（粤语朗读）。

### 视觉风格

配色方案的确定，是整个项目里与 AI 协作体验最好的一环。做法是：让 AI 一次性给出多套配色方案，并用 HTML 页面直接渲染出来「所见即所得」，同时说明每一套配色背后的理由。我再结合自己对岭南色彩的想象，一轮一轮与 AI 沟通，逐步敲定一套真正有「岭南味道」的视觉风格。

### 账号体系

支持**账号 / 用户名 / 邮箱**三种注册登录方式，覆盖不同使用习惯的用户。

## 四、开发难点与解决

### 难点 1：数据库表设计

数据表的结构往往不是一次就能定死的：随着功能的增删，表也会跟着重新设计，期间还牵涉大量**外键与复合键**的问题。

**解法**：先把必要功能定下来，梳理清楚每个核心功能**数据的流入与流出**，把整体框架想通后再开始建表；设计上尽量满足第三范式（3NF），避免数据删除时因结构混乱而相互牵连。

### 难点 2：前端实现——从静态页到 Vue

AI 用纯 HTML 写的初稿往往很漂亮，但要迁移到 Vue 框架里落地，会牵扯出大量技术细节；对一个此前没有前端经验的人来说，哪怕是很小的调整都很折腾。再加上当时大模型对「看效果图复刻」的能力还比较弱，就算把效果图给它看，它也很难真正感知，只能靠一遍遍调整话术来逼近效果。

**解法**：用不同的 AI 交叉调整 prompt，让指令更精准地指向细节错误；同时自己查阅、学习大量前端知识，**边做边学**。后来也发现 Pencil、Figma 这类 MCP 工具已经能让人直接上手编辑页面效果，方向是对的。

### 难点 3：逻辑错误

项目里很多问题其实是逻辑上的混乱：功能与功能之间、代码与代码之间相互牵扯；而当时 code agent 的能力还不够强，尤其当上下文变大、项目膨胀之后，AI 写出来的东西很容易逻辑错乱。

**解法**：先明确「出问题的功能涉及哪些代码文件、哪些函数、数据如何流入流出」，让 AI 先做定位与自省。常用的一个技巧是——**让 AI 先把功能的逻辑完整讲一遍**，很多时候它在复述、梳理逻辑线的过程中，自己就会发现 bug 并默默修好（详见「经验二 · AI 协作」）。

### 难点 4：代码膨胀与「屎山」

项目越做越大、越来越臃肿，AI 写的代码在堆「屎山」。那时候一些 skill 和工具还不成熟，AI 写代码容易「表演专业」、刻意讨好，把简单的事情写得花哨，可很多功能其实只需要引入一个库、甚至一小段函数就够了。

**解法**：在项目规划初期就**严格划分模块**；每完成一个功能，先让它通过构建与测试（CI/CD），再进入下一个模块的开发；同时借助社区里已有的 skill，约束 AI 少写废话。

### 难点 5：长周期开发与存档

整个项目时间跨度很长，中途常常因为没有灵感而停滞很久，再重新「动工」时已有些陌生。

**解法**：每次完成一个模块、取得一点进展，就**实时更新 README 与技术文档**，同时记录下一步的工作打算。既是文档，也是给自己下次重启时的「牵引绳」。

## 五、最终交付

- **整体框架**：Vue + Spring Boot + Redis + MySQL；
- **用户体系**：注册登录、个人主页、关注作者、私信；
- **内容社区**：文章发布、阅读、粤语朗读播放、点赞、评论、收藏；
- **粤文化 AI 助手**：基于 DeepSeek，用粤语解答文化话题；
- **翻译与语音**：粤普互译、粤语语音合成；
- **数据面板**：文章排行榜、运营数据可视化；
- **管理后台**：对网站数据与内容进行统一管理。

## 六、经验总结

我把这段经历沉淀为三层经验，分别对应**技术选型、AI 协作、开发节奏**，以及更上位的**需求管理**与**个人成长**。

### 经验一：技术选型

**为什么选 Vue + Spring Boot？** 说实话，当初并没有特别深度的技术考量，更多是出于两个现实因素：

- **Spring Boot**：市面上已经被大量使用，生态成熟，框架「一键配置」，对一个想快速搭后端个人站的人来说，上手成本相对可控；
- **Vue**：其实不是我有意选的，而是 AI 给的参考建议。当时我对前端框架没有明确偏好，听了分析后觉得 Vue 学习曲线友好，就定了下来。

> 坦白讲，这个选型不是「深思熟虑后的最优解」，而是「在有限信息下做出的合理决定」。

**回头看，有没有更好的组合？** 有的。如果现在让我重新选，我会认真考虑 **Node.js 全栈框架**（如 Nest.js 或 Next.js）：

- 前后端同为 JavaScript/TypeScript，**语言统一**，上下文切换成本低；
- 对个人全栈项目而言，全栈框架**集成度更高**，少操心跨域、打包、路由等配置；
- 整体更「轻」，更适合快速验证想法。

**有没有「杀鸡用牛刀」或「牛刀不够用」的地方？**

- **杀鸡用牛刀**：Spring Boot 对这个体量的项目偏重，事务管理、AOP、安全过滤器链等企业级特性其实只用了皮毛，配置成本却一点没省；
- **牛刀不够用**：遇到非标准化需求（例如粤语 TTS 的音频流处理），Spring Boot 没有现成方案，只能自己封装摸索，反而没有 Node 生态里那么多灵活的库可选。

> 这个选型不是最优的，但在这个项目里「能用」。下一次，我会更看重技术栈的**统一性**与**开发效率**，而不是「看起来工业级」。

### 经验二：AI 协作

**AI 最好用的环节：配色方案生成。** AI 直接生成 HTML 页面来展示不同配色，一目了然。无论是产品经理还是 UI 设计师，用这种方式沟通视觉方案，比口述「我想要岭南风格」要精准得多。

**AI 最不好用的环节之一：布局「微调」。** 没有协作经验的人喜欢用大白话描述布局，比如「这个按钮往左挪一点点」「标题向上走两格」。问题在于——**人类的一点点，和 AI 的一点点，完全不是同一个度量单位**：AI 没有视觉参照系，不知道「一点点」是 2px 还是 20px。

解法：

- 不确定位置时，先让 AI 告诉我**当前元素的大小、位置、与父容器的比例关系**；
- 或者**借助旁边的元素作为参考坐标**，例如「让这个按钮和上面那个输入框左对齐」；
- 用相对明确的指令，替代模糊的「挪一挪」。

**AI 最不好用的环节之二：直接给结果没用。** 一开始我习惯直接说「这个功能跑不通，帮我修好」，但常常修完一个又引出新的问题。

解法：与其直接告诉 AI「结果不对」，不如先问它——**「当前这个功能的触发逻辑是什么？触发后按代码流程应该发生什么？每一步的数据变化是怎样的？」** 让 AI 先把代码逻辑「翻译」成自然语言。在它复述的过程中，它往往自己就会发现 bug 在哪里。我称之为——**「让 AI 自己给自己做 code review」**。

**「先让 AI 复述逻辑、再动手改代码」是怎么发现的？** 这个过程挺有意思：

> 有一次我让 AI 修一个 bug：文章点赞后，详情页的点赞数更新了，但列表页没有。我以为是缓存问题，调了半天 Redis。
>
> 我跟它说：「帮我把点赞功能的逻辑完整解释一遍，从点击按钮到数据库更新、再到页面渲染。」
>
> 它开始复述：「前端点击 → 调用 API → 后端更新数据库 → 返回成功 → 前端更新详情页数据 → 但列表页没有重新请求……」
>
> 复述到一半，它自己停住了：「等一下，这里有问题——列表页的数据来自另一个接口，没有触发重新请求，应该用事件总线或刷新列表。」
>
> **我根本没有让它改代码，它自己在解释的过程中发现了问题。**

从此我养成习惯：遇到逻辑 bug，先让 AI 用自然语言复述「我以为的逻辑」与「实际的逻辑」，让它在复述中发现矛盾，然后才让它改代码。核心是**让 AI 从「解题模式」切换到「讲解模式」，在讲解中自我校正**。

> 一句话概括：**AI 是「懂得很多、但不太负责任」的实习生——你要给它明确的脚手架，让它先讲思路再动手，并时刻盯着它的输出。**
>
> 跟 AI 协作的核心不是「让它帮你写代码」，而是**学会怎么下指令**：用坐标思维替代模糊描述，用「先讲逻辑再改代码」替代「直接修 bug」。

### 经验三：全栈开发节奏

我的节奏倾向是**「先做前端、再做后端」**：

- 前端是「眼睛」——先抓住用户的视觉、做出最小 MVP，人（包括自己）才能感受到「这个产品是有可能实现的」；
- 大部分用户看不懂代码，其实个人开发者也是——**如果看不到页面效果，会觉得代码是死的**。

具体推进方式：

1. 先把基本前端效果做出来（静态页面）；
2. 对功能清单做**优先级排序：核心功能优先，次要功能靠后**。比如一个网站，得先有注册登录，不能一上来就做写文章或 AI 功能；
3. Demo 跑通后，开始**分模块开发**（用户模块、文章模块、互动模块等）；
4. 分模块后就是不断迭代：主要功能先保证可用，次要功能看实现难度再决定做不做。有些功能涉及数据库复杂读写或 Redis 操作，技术难度又会反过来影响优先级——这是一个**动态平衡**的过程。

**下一次会怎么调整？** 仍然会先做前端 Demo（「看得见」对自己也是一种激励），但会更早地**并行设计数据库与 API 结构**，避免前端做到一半发现数据模型要推倒重来；优先级也会更严格——**没有注册登录，就不做任何其他功能**。

> 我的节奏是「视觉驱动 + 优先级排序 + 模块拆分」。先让产品「长出来」，再让它「变强壮」。

### 经验四：需求管理

因为项目还没有正式上线，没有真实用户数据支撑，对需求更多是常识判断：

- **真正需要的**：阅读文章——这是最核心的，用户打开网站就是为了看内容；其次是评论/互动，社区氛围靠它维持。
- **「我觉得很好、但实际可能没人用」的**：写文章功能。好文笔的人是少数，大部分用户是「读者」而非「作者」。内容社区的典型比例是 **1:9 法则**——约 10% 的人生产内容，90% 的人消费内容。所以写文章虽然必须有（不然没有内容），但使用频率注定不高。这不算「做错了」，但需要正视这个现实。

**关于需求验证的反思**：目前没有上线、也没有埋点，所有判断都是「推测」而非「数据验证」。如果正式部署，我会：加简单埋点（至少统计页面访问量与功能点击率）→ 观察哪些功能真的被使用、哪些长期无人问津 → 用数据指导下一轮迭代，而不是靠「我觉得」。

> 需求管理不能靠猜。没上线之前，「我觉得」只是假设；上线之后，「数据说」才是真相。

### 经验五：个人成长

**做项目前 vs 之后**，技术上是实打实的提升：

- **前端**：从「会写静态页面」到「能用 Vue 搭一个完整的 SPA」；
- **后端**：从「写过 Servlet 作业」到「能用 Spring Boot 完成一个完整项目」。

**最有收获的技术点，是 Spring Security 的认证授权流程**，原因有三：

1. **它贯穿了整个项目**：注册（密码加密）→ 登录（生成 JWT）→ 每次请求（Token 校验）→ 接口权限（角色控制），我完整走了一遍；
2. **它让我理解了「什么是真正的后端工程」**：不只是「接收请求 → 查数据库 → 返回数据」，还要考虑安全、会话、过滤器链与异常处理；
3. **它踩的坑最多**：配置类写错一个 `@Order`，整个过滤器链就失效；`AuthenticationManager` 怎么注入、JWT 过期怎么刷新、退出登录怎么让 Token 失效……每一个小问题都让人查大量资料，但每解决一个，对 Spring Boot 的理解就深一层。

第二个让我收获很大的点，是**前后端数据一致性的维护**——比如点赞，前端要同时更新「按钮状态」「详情页点赞数」「列表页点赞数」，这让我真正理解了「数据驱动 UI」。

**但最有价值的成长，不是技术本身**，而是「项目思维」的提升：

- 学会把一个想法**拆解成可执行的功能清单**；
- 学会在有限资源下**做优先级取舍**；
- 学会与 AI 协作完成一个**完整的工程**，而不只是写几段代码。

过程中还有很多「不踩坑就永远不知道」的细节：一个前端容器可能与某个 CSS 组件发生冲突；要先设定好数据结构，前后端才能对齐；一个字段类型不一致，就可能导致整个页面渲染失败。

**关于「AI 时代程序员的角色」**：如今 AI 写代码的能力已经非常强，很多人说「AI 可以取代普通程序员」。但做完这个项目，我越来越确信：

> **AI 可以写代码，但人的价值在于「工程思维」——作为指挥长，去保证项目的质量、数据的安全、功能的完整性。**

写代码只是「执行」；而**定义「要写什么、为什么写、怎么保证不出错」**，才是人要做的事。

> 技术是练出来的，思维是「做项目」磨出来的。做完一个完整的项目，比刷一百道算法题更能让你理解「什么是软件开发」。
